@{
    ViewBag.Title = "BlooddonorList";
}

<!DOCTYPE html>

<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>BlooddonorList</title>
</head>
<body>
    <br />

    <div class="container">  
        <div class="row">
            <div class="col-lg-12">
                <h1>Blood Donors</h1>
                <ol class="breadcrumb">
                    <li>
                        <a href="#">Home</a>
                    </li>
                    <li class="active">BlooddonorList</li>
                </ol>
            </div>
        </div>
        @*<div class="col-md-12">
            <div class="col-md-6">
                
            </div>
            <form class="form-horizontal" role="form" action="/BloodDonation/BloodDonation">
                <div class="form-group col-md-2">
                    <input class="form-control" type="text" name="title" placeholder="Enter Title" />
                </div>
                <div class="form-group col-md-2">
                    <select name="catag" id="catag" class="well-sm">
                        <option value="all">All</option>
                        <option value="AB1">AB+</option>
                        <option value="AB2">AB-</option>
                        <option value="A+">A+</option>
                        <option value="A-">A-</option>
                        <option value="O+">O+</option>
                        <option value="O-">O-</option>
                    </select>
                </div>
                <div class="form-group col-md-2">
                    <input class="form-control btn-info" type="submit" value="Search" />
                </div>

            </form>
        </div>*@
        <div class="col-md-8">

            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Name:</th>
                        <th>Blood Type:</th>
                        <th>Address:</th>
                        <th>Phone No:</th>
                        <th>Email</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var x in Model)
                    {
                        if(x.Name!=null)
                        { 
                            <tr>
                                <td>@x.Name</td>
                                <td>@x.BloodType</td>
                                <td>@x.Address</td>
                                <td>@x.PhoneNo</td>
                                <td>@x.Email</td>
                                @*<td>
                                    <form method="POST" action="/BloodDonation/BloodDonation">
                                        <input type="hidden" name="Id" value="@ViewBag.Id" />
                                        <input type="submit" value="View Details.." class="btn btn-info" />
                                    </form>
                                </td>*@
                            </tr>
                        }
                    }
                </tbody>
            </table>


        </div>
    </div>
</body>
</html>
